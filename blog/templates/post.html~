{% extends "archbase.html" %}
    
{% block content %}

<!--<h2><a href ="/blog/{{post.id}}">{{ post.title }}</a></h2>-->
    
<div class="post_meta">
    on {{ post.created }}
</div>

<div class="post_body">
    {{ post.body|safe|linebreaks }}
</div>
    
<div class="tags">
    {% for tag in post.tags.all %}
    <a href="/blog/tag/{{tag}}" class="class1">{{tag}}</a>
    {% if not forloop.last %} {% endif %}
    {% endfor %}
</div>

<h3><a href="/blog/{{page_obj.previous_page_number}}" class="blognav">Back to Blog</a></h3>
  
{% endblock %}
