{% extends "archbase.html" %}
    
{% block content %}
    
{% for post in object_list %}
    {% if object_list > 1 %}

        <p>{{ post.created|date:'Y-m-d' }}<a href ="/blog/{{post.id}}"> {{ post.title }}</a></p>
    
    {% endif %}
  
{% endfor %}
  
{% endblock %}
